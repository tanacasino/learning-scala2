package com.github.tanacasino.study.hajimeni.future

import scala.concurrent.Future

/**
 * Created by nishiyama on 2015/01/28.
 */
object FutureZipMain {


  def main(args: Array[String]) {
//    val f = Future("Hello")
//    val f2 = f.failed
//
//    val futures = List(1, 2, 3).map(i => Future{
//      if (i == 2) throw new Exception("fail")
//      else Right(i * 2)
//    }.recover{
//      case e => Left(e)
//    })
//    val result = Future.sequence(futures)
  }
}
